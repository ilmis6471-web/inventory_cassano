const fs=require('fs');
const path=require('path');
const src=fs.readFileSync(path.join(__dirname,'server.js'),'utf8');
const fixed="app.put('/api/items/:id',auth,need('items_manage'),upload.single('photo'),async(req,res)=>{try{const old=await one('SELECT * FROM items WHERE id=$1',[req.params.id]);if(!old)return res.status(404).json({error:'Barang tidak ditemukan'});const b=req.body;await run('UPDATE items SET code=$1,name=$2,category_id=$3,price=$4,description=$5,image=$6 WHERE id=$7',[b.code,b.name,b.category_id||null,+b.price||0,b.description||'',imageData(req)||old.image||null,req.params.id]);res.json({ok:true,stock:old.stock})}catch(e){res.status(400).json({error:e.message})}});\n";
const vault="const ensureVault=async()=>{await pool.query(\"CREATE TABLE IF NOT EXISTS vault(id INTEGER PRIMARY KEY DEFAULT 1,balance NUMERIC(18,2) NOT NULL DEFAULT 0,updated_at TIMESTAMPTZ DEFAULT NOW())\");await pool.query(\"INSERT INTO vault(id,balance) VALUES(1,0) ON CONFLICT(id) DO NOTHING\");await pool.query(\"CREATE TABLE IF NOT EXISTS vault_transactions(id SERIAL PRIMARY KEY,vault_id INTEGER REFERENCES vault(id),type TEXT NOT NULL,amount NUMERIC(18,2) NOT NULL,balance_after NUMERIC(18,2) NOT NULL,note TEXT DEFAULT '',user_id INTEGER REFERENCES users(id),created_at TIMESTAMPTZ DEFAULT NOW())\")};\napp.get('/api/vault',auth,async(req,res)=>{try{await ensureVault();const v=await one('SELECT * FROM vault WHERE id=1');const tx=await q('SELECT t.*,u.name user FROM vault_transactions t LEFT JOIN users u ON u.id=t.user_id ORDER BY t.id DESC LIMIT 20');res.json({balance:Number(v.balance),updated_at:v.updated_at,can_edit:['Bos','Consigliere'].includes(req.me.role),transactions:tx})}catch(e){res.status(500).json({error:e.message})}});\napp.post('/api/vault/adjust',auth,async(req,res)=>{if(!['Bos','Consigliere'].includes(req.me.role))return res.status(403).json({error:'Hanya Bos dan Consigliere yang dapat mengedit uang brangkas'});const amount=Number(req.body.amount);const type=req.body.type;const note=(req.body.note||'').trim();if(!Number.isFinite(amount)||amount<=0)return res.status(400).json({error:'Nominal tidak valid'});if(!['IN','OUT'].includes(type))return res.status(400).json({error:'Jenis transaksi tidak valid'});const client=await pool.connect();try{await client.query('BEGIN');await client.query(\"CREATE TABLE IF NOT EXISTS vault(id INTEGER PRIMARY KEY DEFAULT 1,balance NUMERIC(18,2) NOT NULL DEFAULT 0,updated_at TIMESTAMPTZ DEFAULT NOW())\");await client.query(\"INSERT INTO vault(id,balance) VALUES(1,0) ON CONFLICT(id) DO NOTHING\");await client.query(\"CREATE TABLE IF NOT EXISTS vault_transactions(id SERIAL PRIMARY KEY,vault_id INTEGER REFERENCES vault(id),type TEXT NOT NULL,amount NUMERIC(18,2) NOT NULL,balance_after NUMERIC(18,2) NOT NULL,note TEXT DEFAULT '',user_id INTEGER REFERENCES users(id),created_at TIMESTAMPTZ DEFAULT NOW())\");const v=(await client.query('SELECT * FROM vault WHERE id=1 FOR UPDATE')).rows[0];const current=Number(v.balance);const next=type==='IN'?current+amount:current-amount;if(next<0)throw new Error('Saldo brangkas tidak mencukupi');await client.query('UPDATE vault SET balance=$1,updated_at=NOW() WHERE id=1',[next]);await client.query('INSERT INTO vault_transactions(vault_id,type,amount,balance_after,note,user_id) VALUES(1,$1,$2,$3,$4,$5)',[type,amount,next,note,req.me.id]);await client.query('COMMIT');res.json({ok:true,balance:next})}catch(e){await client.query('ROLLBACK');res.status(400).json({error:e.message})}finally{client.release()}});\n";
const sales="const ensureSales=async()=>{await pool.query(\"CREATE TABLE IF NOT EXISTS sales_wallet(id INTEGER PRIMARY KEY DEFAULT 1,balance NUMERIC(18,2) NOT NULL DEFAULT 0,updated_at TIMESTAMPTZ DEFAULT NOW())\");await pool.query(\"INSERT INTO sales_wallet(id,balance) VALUES(1,0) ON CONFLICT(id) DO NOTHING\");await pool.query(\"CREATE TABLE IF NOT EXISTS sales_transactions(id SERIAL PRIMARY KEY,type TEXT NOT NULL CHECK(type IN ('IN','OUT')),amount NUMERIC(18,2) NOT NULL CHECK(amount>0),balance_after NUMERIC(18,2) NOT NULL,order_id INTEGER UNIQUE REFERENCES orders(id),user_id INTEGER REFERENCES users(id),note TEXT DEFAULT '',created_at TIMESTAMPTZ DEFAULT NOW())\");const c=await pool.query(\"SELECT COUNT(*)::int n FROM sales_transactions\");if(c.rows[0].n===0){const s=await pool.query(\"SELECT COALESCE(SUM(total),0) total FROM orders WHERE status='Approved'\");const amount=Number(s.rows[0].total||0);if(amount>0){await pool.query(\"UPDATE sales_wallet SET balance=$1,updated_at=NOW() WHERE id=1\",[amount]);await pool.query(\"INSERT INTO sales_transactions(type,amount,balance_after,note) VALUES('IN',$1,$1,'Saldo awal dari riwayat penjualan')\",[amount])}}};\napp.get('/api/sales',auth,async(req,res)=>{if(!['Bos','Consigliere'].includes(req.me.role))return res.status(403).json({error:'Hanya Bos dan Consigliere yang dapat melihat dana penjualan'});try{await ensureSales();const w=await one('SELECT * FROM sales_wallet WHERE id=1');const tx=await q('SELECT t.*,u.name user,o.order_no FROM sales_transactions t LEFT JOIN users u ON u.id=t.user_id LEFT JOIN orders o ON o.id=t.order_id ORDER BY t.id DESC LIMIT 100');const approved=await q(\"SELECT o.order_no,o.total,o.created_at,u.name requester FROM orders o JOIN users u ON u.id=o.user_id WHERE o.status='Approved' ORDER BY o.id DESC LIMIT 100\");res.json({balance:Number(w.balance),transactions:tx,approved})}catch(e){res.status(500).json({error:e.message})}});\napp.post('/api/sales/transfer',auth,async(req,res)=>{if(!['Bos','Consigliere'].includes(req.me.role))return res.status(403).json({error:'Hanya Bos dan Consigliere yang dapat memindahkan dana penjualan'});const amount=Number(req.body.amount);if(!Number.isFinite(amount)||amount<=0)return res.status(400).json({error:'Nominal tidak valid'});const client=await pool.connect();try{await client.query('BEGIN');await client.query(\"CREATE TABLE IF NOT EXISTS sales_wallet(id INTEGER PRIMARY KEY DEFAULT 1,balance NUMERIC(18,2) NOT NULL DEFAULT 0,updated_at TIMESTAMPTZ DEFAULT NOW())\");await client.query(\"INSERT INTO sales_wallet(id,balance) VALUES(1,0) ON CONFLICT(id) DO NOTHING\");await client.query(\"CREATE TABLE IF NOT EXISTS sales_transactions(id SERIAL PRIMARY KEY,type TEXT NOT NULL CHECK(type IN ('IN','OUT')),amount NUMERIC(18,2) NOT NULL CHECK(amount>0),balance_after NUMERIC(18,2) NOT NULL,order_id INTEGER UNIQUE REFERENCES orders(id),user_id INTEGER REFERENCES users(id),note TEXT DEFAULT '',created_at TIMESTAMPTZ DEFAULT NOW())\");await client.query(\"CREATE TABLE IF NOT EXISTS vault(id INTEGER PRIMARY KEY DEFAULT 1,balance NUMERIC(18,2) NOT NULL DEFAULT 0,updated_at TIMESTAMPTZ DEFAULT NOW())\");await client.query(\"INSERT INTO vault(id,balance) VALUES(1,0) ON CONFLICT(id) DO NOTHING\");await client.query(\"CREATE TABLE IF NOT EXISTS vault_transactions(id SERIAL PRIMARY KEY,vault_id INTEGER REFERENCES vault(id),type TEXT NOT NULL,amount NUMERIC(18,2) NOT NULL,balance_after NUMERIC(18,2) NOT NULL,note TEXT DEFAULT '',user_id INTEGER REFERENCES users(id),created_at TIMESTAMPTZ DEFAULT NOW())\");const sw=(await client.query('SELECT * FROM sales_wallet WHERE id=1 FOR UPDATE')).rows[0];const vw=(await client.query('SELECT * FROM vault WHERE id=1 FOR UPDATE')).rows[0];const sales=Number(sw.balance),nextSales=sales-amount;if(nextSales<0)throw new Error('Dana penjualan tidak mencukupi');const nextVault=Number(vw.balance)+amount;await client.query('UPDATE sales_wallet SET balance=$1,updated_at=NOW() WHERE id=1',[nextSales]);await client.query('UPDATE vault SET balance=$1,updated_at=NOW() WHERE id=1',[nextVault]);await client.query(\"INSERT INTO sales_transactions(type,amount,balance_after,user_id,note) VALUES('OUT',$1,$2,$3,$4)\",[amount,nextSales,req.me.id,req.body.note||'Dipindahkan ke Brangkas']);await client.query(\"INSERT INTO vault_transactions(vault_id,type,amount,balance_after,user_id,note) VALUES(1,'IN',$1,$2,$3,$4)\",[amount,nextVault,req.me.id,'Transfer dari Dana Penjualan']);await client.query('COMMIT');res.json({ok:true,sales_balance:nextSales,vault_balance:nextVault})}catch(e){await client.query('ROLLBACK');res.status(400).json({error:e.message})}finally{client.release()}});\nconst approveSales="app.post('/api/orders/:id/approve',auth,need('approve'),async(req,res)=>{const client=await pool.connect();try{await client.query('BEGIN');await client.query(\"CREATE TABLE IF NOT EXISTS sales_wallet(id INTEGER PRIMARY KEY DEFAULT 1,balance NUMERIC(18,2) NOT NULL DEFAULT 0,updated_at TIMESTAMPTZ DEFAULT NOW())\");await client.query(\"INSERT INTO sales_wallet(id,balance) VALUES(1,0) ON CONFLICT(id) DO NOTHING\");await client.query(\"CREATE TABLE IF NOT EXISTS sales_transactions(id SERIAL PRIMARY KEY,type TEXT NOT NULL CHECK(type IN ('IN','OUT')),amount NUMERIC(18,2) NOT NULL CHECK(amount>0),balance_after NUMERIC(18,2) NOT NULL,order_id INTEGER UNIQUE REFERENCES orders(id),user_id INTEGER REFERENCES users(id),note TEXT DEFAULT '',created_at TIMESTAMPTZ DEFAULT NOW())\");const o=(await client.query('SELECT * FROM orders WHERE id=$1 FOR UPDATE',[req.params.id])).rows[0];if(!o||o.status!=='Pending')throw new Error('Pesanan sudah diproses');const its=(await client.query('SELECT * FROM order_items WHERE order_id=$1',[o.id])).rows;for(const x of its){const i=(await client.query('SELECT * FROM items WHERE id=$1 FOR UPDATE',[x.item_id])).rows[0];if(!i||i.stock<x.qty)throw new Error('Stok tidak cukup')}for(const x of its){await client.query('UPDATE items SET stock=stock-$1 WHERE id=$2',[x.qty,x.item_id]);await client.query('INSERT INTO movements(item_id,type,qty,user_id,reference) VALUES($1,$2,$3,$4,$5)',[x.item_id,'OUT',x.qty,req.me.id,o.order_no])}const sw=(await client.query('SELECT * FROM sales_wallet WHERE id=1 FOR UPDATE')).rows[0];const nextSales=Number(sw.balance)+Number(o.total||0);await client.query('UPDATE sales_wallet SET balance=$1,updated_at=NOW() WHERE id=1',[nextSales]);await client.query(\"INSERT INTO sales_transactions(type,amount,balance_after,order_id,user_id,note) VALUES('IN',$1,$2,$3,$4,$5)\",[Number(o.total||0),nextSales,o.id,req.me.id,'Penjualan disetujui']);await client.query(\"UPDATE orders SET status='Approved',approved_by=$1 WHERE id=$2\",[req.me.id,o.id]);await client.query('COMMIT');res.json({ok:true,sales_balance:nextSales})}catch(e){await client.query('ROLLBACK');res.status(400).json({error:e.message})}finally{client.release()}});\n";
const userDelete="app.delete('/api/users/:id',auth,need('users'),async(req,res)=>{const id=Number(req.params.id);if(id===req.me.id)return res.status(400).json({error:'Tidak dapat menghapus akun yang sedang digunakan'});try{const u=await one('SELECT id,name FROM users WHERE id=$1',[id]);if(!u)return res.status(404).json({error:'User tidak ditemukan'});await run('DELETE FROM users WHERE id=$1',[id]);res.json({ok:true})}catch(e){res.status(400).json({error:'User tidak dapat dihapus karena masih memiliki riwayat transaksi. Nonaktifkan akun tersebut.'})}});\n";
const rolesRoute="app.get('/api/roles',auth,need('roles'),async(req,res)=>res.json(await q('SELECT * FROM roles ORDER BY id')));";
const seededRolesRoute="app.get('/api/roles',auth,need('roles'),async(req,res)=>{await pool.query(\"INSERT INTO roles(name,description,permissions) VALUES($1,$2,$3) ON CONFLICT(name) DO NOTHING\",['Capo','Operasional dan pemantauan',JSON.stringify(['dashboard','items','cart','order','orders','history','stock'])]);res.json(await q('SELECT * FROM roles ORDER BY id'))});";
let patched=src.replace(/app\.put\('\/api\/items\/:id'[\s\S]*?\napp\.delete/,fixed+'app.delete').replace(rolesRoute,seededRolesRoute);
patched=patched.replace("app.post('/api/orders/:id/approve'",approveSales+"app.post('/api/orders/:id/approve'");
patched=patched.replace("app.get('/api/users'",userDelete+"app.get('/api/users'");
patched=patched.replace("app.get('*'",vault+sales+"app.get('*'");
if(patched===src)throw new Error('Patch target not found');
const runtime=path.join(__dirname,'.server-runtime.js');
fs.writeFileSync(runtime,patched,'utf8');
require(runtime);
